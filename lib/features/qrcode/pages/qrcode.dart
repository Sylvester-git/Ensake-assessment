import 'package:flutter/material.dart';

class QrCodePage extends StatelessWidget {
  const QrCodePage({super.key});

  static final routeName = "qr-code-page";
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
