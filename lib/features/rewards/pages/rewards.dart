import 'package:flutter/material.dart';

class RewardsPage extends StatelessWidget {
  const RewardsPage({super.key});

  static final routeName = "rewards-page";

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
